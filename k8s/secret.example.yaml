# Template only — copy to secret.yaml, fill in real keys, and DO NOT commit secret.yaml.
# secret.yaml is gitignored. Apply with: kubectl apply -f k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: studeymate-secrets
  namespace: studeymate
type: Opaque
stringData:
  OPENAI_API_KEY: "sk-..."
  ANTHROPIC_API_KEY: ""
  GEMINI_API_KEY: ""
  DEEPSEEK_API_KEY: ""
  LLAMA_API_KEY: ""
  POSTGRES_USER: "studeymate"
  POSTGRES_PASSWORD: "change_me"
